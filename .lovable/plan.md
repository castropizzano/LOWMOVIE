

# Simplify Intro Buttons

The two buttons ("Enter Portal" and "Skip Intro") do exactly the same thing — navigate to `/home`. One button is sufficient.

## Change

In `src/pages/Intro.tsx`, remove one of the two buttons and keep only **"Enter Portal"** (or "Skip Intro" — whichever you prefer). The single button remains centered at the bottom.

